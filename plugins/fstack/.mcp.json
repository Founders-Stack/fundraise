{
  "mcpServers": {
    "fstack": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/../../packages/fstack-mcp/dist/index.js"],
      "env": {
        "FS_API_URL": "${FS_API_URL:-http://localhost:3000/api}",
        "FS_API_TOKEN": "${FS_API_TOKEN}"
      }
    }
  }
}
