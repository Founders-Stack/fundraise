// Period report + snapshot + execute tools. OWNED BY the W2-distribution workstream.
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";

export function registerDistributionTools(_server: McpServer) {
  // fundraise_report_period, fundraise_snapshot, fundraise_execute_distribution (SPEC 0.3)
}
